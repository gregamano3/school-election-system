# Copy this file to .env.local and fill in your values.
# Database: you must use either PostgreSQL or MySQL (see format below).

# --- PostgreSQL ---
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE
# Example:
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/school_election

# --- MySQL (e.g. XAMPP) ---
# DATABASE_URL=mysql://USER:PASSWORD@HOST:3306/DATABASE
# Example (XAMPP default, no password):
# DATABASE_URL=mysql://root@localhost:3306/school_election
# Example (with password):
# DATABASE_URL=mysql://root:yourpassword@localhost:3306/school_election

DATABASE_URL=postgresql://postgres:postgres@localhost:5432/school_election

# NextAuth
AUTH_SECRET=your-secret-at-least-32-characters-long
NEXTAUTH_URL=http://localhost:3000
# Session expiry in seconds (default: 7200 = 2 hours). User must log in again after this.
SESSION_MAX_AGE_SECONDS=7200
